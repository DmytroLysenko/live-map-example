import styled from "styled-components";

const StyledWatermarksContainer = styled.div`
  select {
    font-family: Inter, san-serif;
    font-size: 14px;
    line-height: 1;
    height: 44px;
    border: 1px solid #c5c5c5;
    border-radius: 8px;
    padding: 12px;
  }
`;

export default StyledWatermarksContainer;
